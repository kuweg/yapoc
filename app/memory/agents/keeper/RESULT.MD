I'll start by reading the current agent-settings.json to understand its structure.I have the full file. Now I'll add `"list_tools"` to the six agents' tools arrays. I'll insert it in a sensible alphabetical position near other list/read tools.

For each agent, I'll place `"list_tools"` after `"file_list"` (alphabetically `list_tools` comes after `file_list`).All six edits applied. Now let me verify with the provided command.Only evaluator shows True. The other five edits must have matched the wrong locations or the file got clobbered. Let me check the current state of the file.Only one occurrence of `list_tools` exists (line 403, in evaluator). The other five edits reported "Replaced 1 occurrence" but apparently the parallel edits clobbered each other — a known issue noted in my memory. Let me check the current state of each agent's tools array.The parallel edits clobbered each other (lost-update). Only the evaluator edit survived. I need to apply the remaining five edits sequentially. Let me read the relevant sections to get exact current context.Now I understand the current state. The five edits (master, planning, builder, keeper, doctor) were all clobbered. I'll apply them sequentially, one at a time, using unique context strings.All five remaining edits applied sequentially. Now verify.All six agents now have `list_tools`. Let me confirm the excluded agents were not touched and JSON is valid.All excluded agents remain unchanged. Task complete.Task complete. I granted `list_tools` to the six required agents (master, planning, builder, keeper, doctor, evaluator) in `app/config/agent-settings.json`, verified all six return `True`, confirmed the excluded agents remain unchanged, and reported back to planning.