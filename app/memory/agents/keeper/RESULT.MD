Let me read the current pyproject.toml to find the exact string to replace.I can see the exact lines. Let me insert `"python-docx>=1.1.0",` between the two entries.Now install it with poetry:Let me verify it's installed:Let me also verify the pyproject.toml looks correct:Poetry normalized the version constraint to `(>=1.2.0,<2.0.0)` — that's fine, it satisfies `>=1.1.0` and is the standard poetry behavior.Done. `python-docx` (v1.2.0) added to pyproject.toml and installed. Poetry normalized the constraint to `(>=1.2.0,<2.0.0)`.