[User source: resume]

I'll delegate this to builder as the resume instructions specify.Let me verify the file directly myself before reporting.Verified. The file exists with exactly the content `DONE`.

**Done.** `projects/post_restart_probe.txt` was created by builder (byte-exact `DONE`, 4 bytes, no trailing newline) and I confirmed it via `file_read`.