Builder completed the task:

- Created `app/tmp/phase25_check.txt` with exact content `VERIFY-OK` (9 bytes, no trailing newline/whitespace).
- Verified via `wc -c` and `od -c`.
- Notified master.

Done.