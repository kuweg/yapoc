I'll research the current state before decomposing. Let me inspect the key files.I've confirmed the wsStore drops prompt and agent. Let me now examine ChatPanel completion synthesis, MessageBubble, and TaskGroupBubble.Let me search within ChatPanel.tsx for the completion synthesis code.Now I understand ChatPanel deeply. Let me look at MessageBubble and TaskGroupBubble to reuse their patterns, and understand the persistence flow (how history messages map to the message insert functions in session store).Now let me understand persistence. The chat history messages are appended via `appendMessage`. To render a TaskCompletionCard on page reload (persisted history), I need to store structured task metadata on the message. Let me inspect the session store and message model.Now I understand the persistence model. Messages persist only `{role, content}` (and attachments), dropping `parts` and any custom fields. This means a TaskCompletionCard rendered from `parts` metadata won't survive a page reload — only `content` text persists.

I need to think about the design carefully. Let me examine the `Message` type in api/types and look at how the backend emits tasks (where task_queue rows emit task_complete). Let me look for backend task emission.Let me find where the backend emits task_complete / task_created events. Let me search the router and services folders.I need to find where task_complete events are actually emitted with their payloads. Let me search for `push_event` with task_complete / task_created across the codebase.Now let me find where the background task execution emits task_update/task_complete/task_error WS events. This likely lives in `app/backend/app/` since there's an `app/backend/app/` folder. Let me look.Let me search the whole backend tree for where task_complete and push_event are used together, and where the queued task dispatcher runs.I need to find where the task_queue rows get their status updated and the WS events emitted. Let me search for `update_queued_task` and `push_event` calls in the codebase (dispatcher, main.py cron runner).I see two main sources of `task_complete` events:
1. `_master_notification_watcher` and `_process_inbox_message` in main.py — these emit `task_complete` with `{session_id, text, source, agent}` but NO task_id/prompt.
2. The task_queue dispatcher which processes rows.

Let me look for where the task_queue dispatcher runs and emits events. Let me search `dispatcher.py` and the main.py cron/_dispatcher_tick. Let me look for `push_event("task_complete"` and `update_queued_task` usage.Now let me find the task_queue dispatcher that actually processes `pending` tasks → runs agent → updates status to done/error → emits `task_update`/`task_complete`/`task_error`. Let me search the backend for where task_queue status transitions happen and events emitted. Let me grep for relevant code in `app/utils/dispatcher.py`.

Let me look at the dispatcher file, which was in files_touched list for this feature branch more broadly.The files_touched list indicated `dispatcher.py`. It's probably under `app/backend/services/`. Let me find it.Let me find where the dispatcher runs `task_queue` rows. Search main.py for the queue-processing/cron dispatcher tick. Let me look lower in main.py and grep the codebase for "push_event" with task_update/task_complete. Let me search memory tools won't help for code. Let me grep via reading key files. Let me search for where `get_tasks_by_status('pending')` or `update_queued_task(... done/error)` and event emission happens during agent run.

Let me look at how a queued task (like from cron, evaluator, goals) gets executed. Search main.py for "_dispatcher_tick" or queue watcher.