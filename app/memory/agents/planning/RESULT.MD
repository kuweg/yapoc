Let me examine the surrounding usage — how `_notify_master`, `resolve_agent`, and other references to these result fields are consumed. Let me look at what other code references `approved_plan`, `escalation_summary`, `COUNSELOR_ROLES`, and how the concilium agent uses things.

Let me check the concilium agent directory and other consumers.This confirms the stale `last_result.json` (session bac4dcf1 from June 7). Let me search for any other code consumers referencing `approved_plan` / `escalation_summary` to assess backward-compat risk, and check how the concilium agent drives the tool.Let me grep for references to the old result fields and the concilium tool across the codebase to understand who consumes these fields.Let me grep for code references to `approved_plan` and `escalation_summary` fields across Python files (outside concilium.py) to understand backward-compat risks. Also let me look at how the concilium agent drives deliberation (PROMPT.MD) and other consumers.

Let me search the codebase for consumers of these dataclass fields with a file search on likely consumer files.Now I have a clear understanding. Let me use grep to verify there are no other Python code consumers that read `result.json` payload fields (`approved_plan`, `escalation_summary`) or index at the concilium result files, and check how `_write_status` current_round/max_rounds is consumed by the router/UI.

Let me search for references to these in code.Now let me search for any Python consumers of the result.json payload or the `DeliberationResult` dataclass fields outside concilium.py — e.g., router code that lists sessions, and anything reading `approved_plan`/`escalation_summary`. Let me check whether there is a "concilium sessions" router/endpoint that reads result.json.I don't have a grep tool directly, but the registry references a `GrepTool`. Let me instead find the router/backend code that surfaces concilium sessions to understand whether UI consumes `result.json` fields. Let me find code referencing "concilium" across the backend.

Since I have file_list and file_read only, let me search strategically. Let me check where concilium sessions are surfaced to the router/UI.This is the backend. There's a `concilium_router` in `app/backend/routers`. Let me check what it does — it may read result.json files and expose them to the UI. This is critical for the backward-compat analysis of the new `result.json` schema.

Let me read the concilium backend router.The import in main.py is `from app.backend.routers import (... concilium_router ...)`. Let me find the actual file.The routers package is under `app/backend/routers/`. The main.py imports at line close to end reference `concilium_router` from that package. Wait, but the import line shows `from app.backend.routers import (...)`. But the file was not found at that path. Let me check the routers dir.The file is `concilium.py` (the import alias uses `concilium_router` because of how routers are named/module variables). Let me read it — this is the UI-facing surface that reads result payloads.Now I have the complete picture. Key findings for backward-compat:

**Concilium backend router** (`app/backend/routers/concilium.py`) — constrains my design:
1. Imports `CONCILIUM_DIR`, `ConciliumOrchestrator`, `DeliberationResult`, `DeliberationStatus`, `COUNSELOR_ROLES` directly from `app.utils.concilium`. So `deliberate()` signature, `session_id`, `rounds`, `max_rounds`, `active_roles` attributes, and the `_write_status` helper are used by the router.
2. `_write_status(session_id, state, current_round=..., max_rounds=..., active_roles=..., error=...)` — local import used. Must keep signature working.
3. `DeliberationResult` and `DeliberationStatus` symbols are imported — must keep existing names.
4. `COUNSELOR_ROLES` keys are used as default roles list in `_session_info`.
5. The router calls `orchestrator.rounds`, `orchestrator.max_rounds`, `orchestrator.active_roles` in background error handler. So `rounds`, `max_rounds`, `active_roles` must remain attributes (or the error path be updated). This is backend Python which I'm allowed to touch — but the task says keep changes to concilium.py + concilium_deliberate.py ideally. The router is backend Python and I can update it if needed, but task scopes to the two files. However the router's error path accesses `orchestrator.rounds`. To be safe and not break UI, I'll keep `rounds`, `max_rounds`, `active_roles`, `session_id` attributes on the orchestrator.

The frontend ConciliumTab reads `current_round`/`max_rounds`/`state` from STATUS.json and reads result via `POST /deliberate` return and `GET /result/{session_id}` (full JSON). The task says prefer NOT to touch UI. So result.json shape matters: UI reads whatever the result endpoint returns. But the deliverable says change the result.json shape. Since there's no frontend reading specific fields per docs (the ConciliumTab would render generically from new fields if it handles missing keys gracefully), and the task explicitly constraints "no UI/frontend changes unless truly required", I need to check the UI doesn't hard-depend on `approved_plan`/`escalation_summary`. 

Given the user explicitly says prefer NOT to touch UI, and the new schema keeps `status/session_id/rounds_completed/duration_s/total_cost_usd` plus replaces `approved_plan`/`escalation_summary`. I'll delegate but first check the full concilium.py to design precisely.

Let me read the current full concilium.py.Now I have the complete source of `app/utils/concilium.py`. Let me read the tool file `concilium_deliberate.py` fully to understand its exact return shape.