# Project Decisions
[2026-05-23] Master timeout increased to 900s (was 300s)
[2026-05-23] Evaluator now verifies configs against live files before reporting
[2026-05-23] Observability metrics filter to last 24h + ERROR-only
[2026-05-23] Moonshot per-chunk timeout reduced to 60s
[2026-05-23] wait_for_agent default timeout bumped to 900s
[2026-05-23] Session checkpointing via SUMMARY.json on restart
[2026-09-07] Knowledge-store ownership: shared/KNOWLEDGE.MD is the single source of truth for project knowledge (only write target, always-on in context, indexed as agent='shared'). project/DECISIONS.md + CONVENTIONS.md are structured retrieval-only stores (indexed as agent='project', NOT injected). project/KNOWLEDGE.md was a stale partial migration copy causing double-injection + double-indexing — DELETED (file + 10 stale vector entries). Do not recreate it.

[2026-09-07] Memory decay engine — full design (scoped, phased). Current state: app/utils/memory_decay.py exists but only covers per-agent MEMORY.MD by binary age cutoff (30d), archiving to data/memory_archive/ (write-only, never indexed/read). Four gaps: (1) coverage — shared/KNOWLEDGE.MD (50-entry cap), NOTES.MD, LEARNINGS.MD (20-rule cap) never age out; (2) signal — pure age, no supersession/recency/category-rate; (3) cold tier is a black hole — archive never indexed or searched; (4) no indexer re-sync after archive. Target design: two-tier (hot=cold-searchable, injected vs searchable-only) + per-category decay score = f(age half-life, last_retrieved recency, supersession). Phases: P1 generalize archive_stale_memory to shared/NOTES/LEARNINGS + prune superseded shared entries + indexer re-sync (unblocks cap); P2 add tier column (migration) + index data/memory_archive as tier=cold + search_memory include_cold flag (default off); P3 scored decay (half-life + recency + supersession auto-mark) replacing binary cutoff; P4 observability (tier counts in metrics) + optional injected-chars budget backstop.

[2026-09-07] Memory decay Phase 1 DONE. (1) PRUNE: shared/KNOWLEDGE.MD 68→62 entries — 6 explicitly-superseded entries (2× April model-optimization, "master on kimi-k2.6", "current model distribution 14 agents", Concilium 3b0b4665 APPROVED-but-pending, evaluator truncation cluster abated) archived to data/memory_archive/shared.archive.md. (2) GENERALIZE: new archive_stale_shared_knowledge(max_age_days, dry_run) in memory_decay.py — parses `## Entry:` blocks by `**Time:**` field, keeps undated entries, archives old to shared.archive.md; wired into _memory_decay_tick() in main.py (after per-agent sweep, try/except guarded). (3) INDEXER re-sync is automatic via 10-min _indexer_tick (hash-based index_shared_knowledge). NOTE: builder returned silent-empty "[] OK:" (documented failure mode) with ZERO changes — master implemented directly and verified (ast.parse OK + dry-run=11 entries eligible at 30d). P2 (cold tier searchable) still open — the 11 entries the 30d decay will archive are not yet searchable until the archive is indexed.
