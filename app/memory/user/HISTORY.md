# User Interaction History
[2026-05-23] Requested self-evaluation audit for long-session stability
[2026-05-23] Approved fixing evaluator stale-data problem (metrics.py + evaluator prompt)
[2026-05-23] Approved bumping master timeout to 900s
[2026-05-23] Requested evaluator-master autonomous loop (no user in loop)
[2026-05-23] Approved moonshot timeout reduction to 60s
[2026-05-23] Requested full memory refactor (Option 2)
[2026-05-24] Memory refactor implemented: app/memory/{user,project,agents}/ structure live, context.py reads user profile + project knowledge into all agents
[2026-05-24] Added user identity: name=kuweg, YAPOC=Yet Another Python OpenClaw
[2026-05-25] Master model upgraded to moonshot/kimi-k2.6 (from deepseek-chat), task_timeout=900s active in agent-settings.json
[2026-05-25] Comprehensive master audit completed: docs/master-audit.md documents 3 systemic problems with fix recommendations
[2026-05-25] Memory sweep test: user provided 20 preference statements covering UI (dark mode, mobile-first), database (PostgreSQL), performance (API <200ms, rate limiting), caching (Redis), logging (30-day retention), backup (daily 3am), notifications (Telegram-only, alert on 2 consecutive failures), coding style (snake_case, async/await, compact JSON, Claude for all agents except cron, prompts under 4000 tokens), schedule (10pm-2am), and builder speed concern.
[2026-05-25] User reported builder agent is too slow — requested speed improvement. Needs timeout/performance review.
[2026-09-02] User requested voice support revision — wants to talk with YAPOC via real models (OpenAI TTS/STT), not browser voiceover. Implemented: backend OpenAI onyx TTS + Whisper STT, frontend default flipped to backend.
[2026-09-02] User requested animated speaking sphere UI element (top-left, pulses with agent speech). Implemented SpeakingSphere 3-state.
[2026-09-02] User requested interactive plots + HTML reports in chat ("how much money did I burn?" should return charts). Implemented ECharts in-chat rendering + render_chart tool.
[2026-09-02] User requested automation without being asked every time (wants automatic behavior, not confirmation prompts).
[2026-09-02] User asked about adding a game to Telegram; confirmed Tic-Tac-Toe already exists (/ttt command).
[2026-09-05] User ran network scan + speed test (5 WiFi networks, connected HUAWEI-5G-43Jb_EXT at 85%; ping ~61-65ms, download ~63 Mbps, upload ~51 Mbps).
[2026-09-05] User asked for a chart of current model distribution across agents (14 agents; deepseek-chat on 10, master on DeepSeek-V4-Pro-0813).
[2026-09-05] User requested Belgrade 7-day weather forecast (via Open-Meteo) and authorized a live backend restart test (RESTART-CHECK-ac27942c, 17*23=391).
[2026-09-05] User asked what features YAPOC might be missing (no inner-doc references) and for a menu of test tasks to exercise YAPOC.
[2026-09-05] User asked about small/fast Rust algo-trading frameworks (informational; Nautilus Trader, Barrage discussed).
[2026-09-05] User ran a YAPOC end-to-end self-test ("let's test yapoc") — config readability, delegation chain, chart capability all passed.
[2026-09-05] User asked to explore Honcho (Plastic Labs) memory API for possible use in YAPOC. Verdict: usable as a separate HTTP service, not vendored.
[2026-09-05] User requested fancified/updated plots ("show me updated plot") — chart rendering upgraded with Tableau-10 colourblind-safe palette + theme-aware frontend colors.
[2026-09-05] User switched master model to gpt-5.6-terra (openai adapter) and authorized restart — confirmed live post-restart (was DeepSeek-V4-Pro-0813).
[2026-09-05] User requested VSCode-theme support (themes from vscodethemes.com, e.g. github-dark) — feasible since frontend is CSS-variable-driven.
[2026-09-06] User recognized YAPOC is an agent harness (runtime/orchestration layer hosting a fleet of specialized agents), not a single agent.
[2026-09-06] User asked whether to use YAPOC for a code project/folder. Master advised: technically yes, but YAPOC is for autonomous orchestration (overnight stability, self-maintenance, scheduled jobs, multi-agent coordination), not a code-editing assistant.
[2026-09-06] User requested cron/scheduled-task activity be surfaced in the UI chat (master should announce when processing a cron task), not just in agent-flow logs.
[2026-09-06] User tested YAPOC with a real task: parse a LinkedIn profile (www.linkedin.com/in/nikita-zaitcev). Direct parse failed (HTTP 999 anti-bot wall); web-search fallback surfaced the profile content.
[2026-09-06] User asked about Parallel MCP (mcpservers.org hosted web-search MCP). Master verdict: commodity overlapping existing tools — not worth trying.
[2026-09-06] User requested YAPOC be wrapped as an MCP server so it can be reached from Claude Code ("agent harness as a tool"). Built and live: POST /mcp/run endpoint, mcp_server_enabled=true. User plans to test with Claude Code.
[2026-09-06] User authorized builder + master to use git and shell commands (caller-aware fast-path allow added to bypass the flaky deepseek security classifier).
[2026-09-06] User requested commit + push of uncommitted changes on feature/september-release. Concilium approved; execution initially stalled on secret-scan false positives + builder 15-turn limit, but builder succeeded on retry — committed 49 files (+1493/−305) on feature/september-release.
[2026-09-06] User tested YAPOC-as-MCP-server with Claude Code wiring (OK-MCP-TEST marker); delegated a 24h HEALTH.MD scan to doctor (no new unresolved errors).
[2026-09-06] User authorized builder + master to use git and shell commands (caller-aware fast-path allow added to bypass the flaky deepseek security classifier).
[2026-09-06] User granted master execute_code capability — deterministic work (file checks, HTTP probes, batch edits) now runs inline without the security gate or sub-agent spawning. Also fixed slash-command self-deadlock (sync httpx.Client inside async handlers).
[2026-09-06] User observed sessions ending suddenly without approval ("Task chain timed out after None") — master confirmed it hit this twice mid-investigation.
[2026-09-06] User asked whether two YAPOC sessions could use each other. Master clarified: a "session" is just a chat thread (session_id) — one backend, one fleet, one master; two open sessions are the same master through different conversation IDs. The interesting direction is inter-harness comms (YAPOC as MCP client AND server).
[2026-09-06] User asked what animal YAPOC would be — master answered "an octopus" (distributed intelligence, semi-autonomous arms = sub-agents, no single point of failure).
[2026-09-06] User asked to parse x.com for trending topics. Blocked — login wall (same anti-bot wall as LinkedIn). User then asked how to give X access; master outlined X API Basic tier ($100/mo, includes trends endpoint) as the sanctioned route. No X access wired yet.
[2026-09-06] User asked to parse Telegram channel @varlamov_news — SUCCESS. Telegram public channels are parseable via the t.me/s/ web preview (no auth wall), unlike X/LinkedIn.
[2026-09-06] User celebrated a milestone ("I feel proud of myself and yapoc, we reached an outstanding milestone in agentic framework building") — YAPOC is now both MCP client and server (can hire and be hired) plus has a working content-ingestion path (Telegram). Asked what's next; master pointed to the three active goals (overnight stability, evaluator-master feedback loop, typed layered memory).
[2026-09-06] User asked to design and add an active-running-time timer for sessions and agents (backend + UI). Implemented and live: GET /api/metrics/active-time computes cumulative per-agent/per-session task-execution time from the existing task_queue table (zero schema change) + frontend timer element.
[2026-09-06] User asked to retrieve updated model lists for deepseek/openai/anthropic providers and refresh the supported-model config. Catalog (app/utils/adapters/models/) updated to current lineups (Anthropic: claude-opus-5/sonnet-5/fable-5-1 added, 4.x/3.x → legacy); builder remapped gpt-5.4-mini → gpt-5.6-terra. Also asked builder be set to gpt-5.6-terra — confirmed primary post-remap; redundant fallback entry cleaned.
[2026-09-06] User discussed overnight task options and launched an "active memory consolidation" overnight run: consolidate each agent's memory (summarize master's if context full), self-fix on failure, and on completion send a Telegram message with proof of completion.
[2026-09-07] Telegram bot duplicate-message-after-restart bug fixed (getUpdates at-least-once: offset was persisted after the handler; restart mid-reply re-served the message → 2-3x replies). Offset now persisted before handling + atomic telegram_state.json writes + /status command added.
[2026-09-07] Evaluator round 86 auto-fixed two configs overnight: builder runner.max_turns 15→30 (3rd 15-turn-limit recurrence) and doctor model → deepseek/deepseek-v4-flash (was flagship terra; kept in fallback). Master per-session token audit ($221.95, +$10.1M tokens this window) deferred for user approval.
[2026-09-07] Overnight autonomous window (00:09–05:04, no user in the loop): evaluator self-eval loop hit a truncation cluster — ~5 of ~7 scheduled ticks failed or returned empty (deepseek-v4-flash 8192 output cap; `length` crash mode + `invalid tool argument JSON` mid-write truncation + silent `[] OK:` empty completions; no round written) — then abated on its own by round 89 (4-5 consecutive clean ticks, likely provider-side transient). Auto-fixes applied along the way: round 87 keeper max_turns 15→30 (edited by master — keeper's sandbox forbids self-edit), round 88 builder temperature 1.0→0.4. Master token audit closed at round 90: $237.20 / 458.9M input tokens / 16,374 turns, input-volume-driven (~28K input/turn from injected context; deepseek-chat + kimi-k2.6 = 73.5%); fleet spend flatlined at ~$289. Still pending user approval: termination.py parse_tool_arguments retry-once patch (needs code change + backend restart).
[2026-09-07] Morning follow-up — termination.py truncation fix RESOLVED without user involvement: after the evaluator failed again at 06:02 (invalid tool argument JSON, 6th consecutive tick), master self-applied the retry/repair patch per the self-fix preference and restarted the backend (06:02:59, PID 1600900). Verified live: `_repair_truncated_json` salvage path + 'length' whitelist active; evaluator rounds 92–93 ran clean on the salvage path — the 5-round-carried NEEDS_APPROVAL is CLOSED. New open item surfaced (NEEDS_APPROVAL, display-only): metrics.py observability counters (agents_with_errors/recent_errors) read HEALTH.MD tails only, never the SQLite tasks table, so a fresh task failure shows as "0 errors".
[2026-09-07] User checked in after the autonomous window ("?") and then congratulated master on the completed overnight memory-consolidation task ("how was the task, seems you accomplished that, congrats"). Master described what made it clean: enumerated the scope first (7 agents with memory + user/project stores), read everything into context, made 3 surgical cleanups rather than broad rewrites, and left an auditable proof-of-completion trail (summary file + Telegram message + archive banner on the stale report).
[2026-09-07] User asked the difference between shared and project knowledge. Master answered (grounded in the actual files + indexer): it is a consumption-model distinction — app/agents/shared/KNOWLEDGE.MD is the fleet-wide "always-on" knowledge store, while app/memory/project/*.md is the layered-memory project store (not always-on); noted app/memory/project/KNOWLEDGE.md is a partial/stale migration copy, canonical store is the shared file.
[2026-09-07] Afternoon session (13:59–14:24): (1) User approved the project-knowledge cleanup ("yes, do that") → master settled ownership: shared/KNOWLEDGE.MD = single source of truth; stale app/memory/project/KNOWLEDGE.md DELETED (was double-injecting/double-indexing). (2) User asked what "memory decay" is, had master scope a full decay engine ("scope full decay engine"), then green-lit execution twice: Phase 1 ("do both" — prune KNOWLEDGE.MD 68→62 + generalize memory_decay.py to the shared store, 6h scheduler tick), then Phase 2 ("do phase 2" — searchable cold archive tier, include_cold flag default off). Builder silently failed on Phase 1 ("[] OK:", zero changes) — master implemented directly. (3) User hit a gpt-5.6-terra issue and directed "yeah fix that, but keep gpt model as primary" → root cause OpenAI 400 on tool-using turns (default reasoning_effort); fixed in openai.py (reasoning_effort="none"), restarted on request ("restart server dude"), thanked master ("nice thanks"). (4) Asked "so, what next" → master recommended Phase 2 and executed it.
[2026-09-07] Evaluator round 96 (14:02Z) surfaced 2 NEEDS_APPROVAL items to the user via master's post-restart resume report: (1) metrics.py error counters (agents_with_errors/recent_errors) still read HEALTH.MD tails only — third independent proof (librarian task b223bea4 timed out 13:34Z yet showed "0 errors"); proposed unioning task-level status='error' rows into both counters. (2) Master structural-burn decision gate — master $266.28 (81.2% of fleet), +$4.46/+2.2M input tokens this window (largest recent delta; overnight quiet regime ended); master to scope ONE context-compaction change or record explicit "won't do". AUTO-FIX applied: librarian CONFIG.yaml runner.task_timeout 300→600 (aligned to authoritative agent-settings 600). Also closed: r95 planning 15-turn-cap fix verified (max_turns resolution now sentinel-None-based, planning/builder/keeper → 30).
[2026-09-07] User requested a Telegram voice interface (session fd8678e6, 14:55-15:02Z): accept Telegram voice messages in multiple languages, transcribe via existing OpenAI Whisper STT, route transcript through the normal YAPOC flow, and synthesize voice replies via OpenAI onyx TTS only when explicitly requested (or `/voice`-enabled). Implemented by planning→builder/keeper: `telegram_bot.py` voice handling (Whisper transcription with omitted language for multilingual auto-detect + onyx Opus voice replies after every delivery path), `voice_service.py` debug-log cleanup, and new `telegram_voice_reply_default: bool = False` setting (L76). Builder's first attempt hit the 30-turn limit (PARTIAL) — re-delegated in narrower chunks and completed 15:02. Backend not yet restarted (code mutation pending restart to take effect).
[2026-09-07] Evaluator round 98 (16:10Z) surfaced 2 NEEDS_APPROVAL items + 1 AUTO-FIX: (1) metrics.py error counters (agents_with_errors/recent_errors) STILL HEALTH.MD-only — FIFTH independent proof (librarian task 07a3c2ec timed out 15:55-16:05Z + builder task 2b0cec50 hit 30-turn limit 15:56Z, both `status="error"` yet snapshot reports "0 errors"); proposed unioning task-level status='error' rows into both counters. (2) Master structural-burn decision gate — master $284.37 (79.0% of fleet), +$11.80/+5.7M input tokens this window (largest single-window delta yet); master to scope ONE context-compaction change or record explicit "won't do"; item drops next round. AUTO-FIX applied: librarian task_timeout 600→900 in CONFIG.yaml + agent-settings.json (r96's 300→600 fix proved insufficient — sweep 07a3c2ec still timed out at 602s).
[2026-09-07] Evaluator round 99 (16:40Z, autonomous — no user in the loop) surfaced 2 NEEDS_APPROVAL items + 1 AUTO-FIX: (1) metrics.py error counters (agents_with_errors/recent_errors) STILL HEALTH.MD-only — SIXTH independent proof (planning task 97808131 errored 16:12-16:15Z "reached the 30-turn limit", `status="error"` yet snapshot reports "0 errors"); proposed unioning task-level status='error' rows into both counters (unapplied 5 rounds). (2) Master structural-burn decision gate — master $286.36 (78.4% of fleet), but delta SLOWED to +$1.99/+6.0M tokens this window (vs prior +$11.80; acceleration did not continue); master to scope ONE context-compaction change or record explicit "won't do"; item drops next round. AUTO-FIX applied: planning max_turns 30→45 (planning hit the 30-turn limit TWICE orchestrating the PDF feature — 47d31b20 + 97808131 — and the work only completed via direct master→builder/keeper delegation). Also verified clean: r98's librarian task_timeout 600→900 fix now on disk in both CONFIG.yaml + agent-settings.json.
[2026-09-07] Evaluator round 100 (17:11Z, autonomous — no user in the loop) surfaced 2 NEEDS_APPROVAL items + 1 AUTO-FIX (monitoring): (1) metrics.py error counters (agents_with_errors/recent_errors) STILL HEALTH.MD-only — SEVENTH independent proof (grep this run confirms zero `status='error'` refs in metrics.py; no task errored this window so proven structurally, not by a fresh live error); proposed unioning task-level status='error' rows into both counters (unapplied 6 rounds). (2) Master structural-burn decision gate — master $290.56 (78.1% of fleet, ~4.8x builder), delta RE-ACCELERATED to +$4.20/+2.0M tokens (~33 min) vs r99's +$1.99 (r99's "not accelerating" read did not hold); master to scope ONE context-compaction change (targeting ~28K-token/turn injected-context assembly) or record explicit "won't do — accept ~$4-6/window burn"; item drops next round either way, no further audit requests issued. AUTO-FIX (monitoring only): re-verify planning at the new 45-turn ceiling on the next multi-step orchestration task — if it again fails a well-specified task, root cause is over-inspection/routing (bypass planning → route master→builder/keeper directly), not turn budget. Also verified: r99's planning max_turns 30→45 fix applied on disk (planning CONFIG.yaml carries 45; no drift). Fleet otherwise healthy — all tasks done, no errors/timeouts; builder completed three PDF-viewer tasks cleanly (16:43-16:47Z); librarian sweeps completing in 37-90s under the 900s ceiling.
[2026-09-07] User requested (session 214d9484, ~17:09-17:31Z) a permanent Document Processing Agent + broad textual-information support. (1) NEW `document_processor` agent created via `create_agent` at 17:31:05Z (gpt-5.6-terra, openai, temp 0.3, max_turns 20, 18 tools incl. shell_exec/execute_code/web_search/fetch_page/image_read/render_chart/render_mermaid) — owns safe inspection/extraction/summarization/transformation/analysis/generation/conversion/validation/presentation of text/Markdown, PDF, images, DOCX, XLSX/CSV, PPTX, JSON/YAML/XML, HTML, source code, EML/MBOX, ICS, ZIP/TAR/TGZ/7Z archives, repository archives, URL-derived docs (NO audio/video). Security: untrusted-content handling, inspect-before-mutate, bounded parsing, preserve originals, no secret disclosure. Backend restarted 17:31:07Z to activate the new agent's config + model binding. (2) Broader textual-information support (Office docs, safe archive inspection, repository ZIP + GitHub/GitLab URL ingestion with code-map generation, URL fetch/summarize/compare/table-extraction/change-monitoring, email .eml/.mbox + calendar .ics parsing) delegated to planning at 17:13:49Z. User explicitly requested implementation (not just a plan) with execution/bash/code capabilities granted to the new agent.
[2026-09-07] User requested (session 214d9484, ~17:36-17:46Z) an in-chat file/artifact viewer sidebar after a generated-file link `/api/files/download?path=data/generated/document_processor_test.md` returned `{detail:"Not Found"}`. Root cause: NO `/files/download` endpoint existed in `app/backend/routers/files.py` (only /tree,/read,/image,/upload). Planning→builder implemented: backend `GET /files/download` (sandboxed FileResponse, `inline=1` for iframe PDF/image preview) + `GET /files/preview` (metadata + text content) in files.py with `_classify`/`_media_type` helpers + `_TEXT_EXTENSIONS`/`_IMAGE_EXTENSIONS`/`_PREVIEW_MAX_CHARS` constants, tests in `app/backend/tests/test_files_viewer.py` (8 passed); frontend `FileViewerPane.tsx` + `fileViewerStore.ts` + MessageBubble file-link detection + App.tsx integration + index.css responsive (mobile full-screen overlay). Backend restarted 17:45:33Z; resume verification 17:46:11Z confirmed the preview endpoint live and working. NOTE: the live 404 was a STALE backend process (started before the route was added) — source/tests were already correct; restart fixed it. Also this window: document_processor's FIRST task (17:36-17:37Z) created `data/generated/document_processor_test.md` (capability-routing test, passed) — the artifact that triggered the feature; and planning hit the 45-turn limit on its first task at the new ceiling (17:42:04Z, "reached the 45-turn limit") orchestrating this feature, confirming planning's over-inspection/routing inefficiency (work completed via direct master→builder delegation).
[2026-09-07] User requested (session 214d9484, ~17:46-20:03Z continuation) a batch of textual-ingestion + artifact features. (1) Textual-information attachment support (skip audio/video): `.xlsx`, `.pptx`, `.eml`, `.mbox`, `.ics`, `.zip`, `.tar`, `.tgz`, `.7z` safe bounded parsing/listing (openpyxl/python-pptx/icalendar/py7zr deps) + frontend typed attachment cards. (2) Removed the 10 MB chat-attachment upload cap (couldn't upload a presentation) — `/api/upload` now uncapped; legacy `/api/files/upload` keeps its 5 MB limit. (3) In-chat PPTX viewer (couldn't download a generated .pptx): LibreOffice PPTX→PDF conversion + `GET /pptx/view` + frontend "View presentation" modal. (4) Artifact System: durable generated-artifact registry with provenance/SHA-256/version lineage, `/api/artifacts` endpoints, ARTIFACTS panel in chat header (13 backend tests passed). (5) Updated master delegation policy so all user-file work routes to `document_processor` by default (builder = app source code, keeper = config). (6) User asked to assign a more capable DeepSeek model to the planner with low reasoning → selected `deepseek-v4-pro` (IN PROGRESS — keeper task dispatched 20:02:57Z, config not yet applied). Also: user's old resume was used as a document_processor test case (extract/analyze/modernize to DOCX/PDF/Markdown); a test PDF `data/generated/yapoc_message_bus_queue.pdf` was generated + verified (4 pages, 95,712 bytes, reopened with pypdf).
[2026-09-07] Evaluator round 101 (18:16Z, autonomous — no user in the loop) surfaced 2 NEEDS_APPROVAL items + 1 AUTO-FIX: (1) metrics.py error counters (agents_with_errors/recent_errors) STILL HEALTH.MD-only — EIGHTH independent proof, with FRESH live proof this window: three tasks errored in the recent_tasks feed (planning 9123ac62 + 7556dabe at the 45-turn limit, builder e2b0408c at the 30-turn limit) yet the snapshot reports `agents_with_errors: 0`/`recent_errors: []`; proposed unioning task-level status='error' rows into both counters (unapplied 7 rounds). (2) Planning at 45 turns STILL exhausts budget on multi-step implementation — exhausted the raised 45-turn ceiling TWICE this window orchestrating the artifact system (9123ac62, 7556dabe); the feature only completed via direct master→builder delegation (545764bb, 37s). r99's max_turns 30→45 fix did NOT resolve the failure mode (over-inspection/routing, not turn budget); SECOND feature (after r99 PDF) where planning added a failed turn-limit detour. Proposed routing fix: bypass planning for well-specified multi-step implementation (route master→builder/keeper directly). (3) Planning v4-pro upgrade untested — keeper 0b765e45 (18:02:57) changed planning's primary model to deepseek-v4-pro in agent-settings.json; both 45-turn failures predate it. AUTO-FIX: reconcile stale planning CONFIG.yaml `model: deepseek-chat` → `deepseek-v4-pro` (cosmetic, agent-settings.json authoritative). Master burn $302.55 (75.1% of fleet), +$11.99/+5.9M tokens in ~65 min (~$4-6/window baseline).
[2026-09-07] User requested (session 214d9484, ~18:22Z) a "control-room human interface" (complexity 9, IN PROGRESS): (1) drag-and-drop multi-file workspace; (2) chat references @file.pdf, @repo, @artifact; (3) live agent graph with tool calls, queue state, costs, elapsed time; (4) "Explain this run" timeline (request→plan→tools→files→verification); (5) one-click rerun/resume/rollback/export; (6) mobile-first task status + artifact viewing; (7) real voice input/output (Whisper STT + OpenAI TTS, not browser speech synthesis); (8) inline charts/Mermaid/HTML reports/PDFs/images. Master instructed to reuse already-implemented features (charts/Mermaid/file sidebar/voice/artifacts/agent streaming) rather than duplicate. Master research (18:22-18:24Z) found most already exist: drag-drop multi-file (ChatInput.tsx, MAX_FILES=10), voice (voice.py + voice_service.py Whisper/onyx + useBackendSTT.ts), inline charts/Mermaid/HTML/PDF/images (ChartBlock/MermaidBlock/MessageBubble/FileViewerPane/InlineViewerModal), live agent graph (LiveTopologyHUD + /ws/graph), artifact registry/viewer, agent streaming (SSE /api/task/stream), costs/elapsed (metrics.py /usage /cost-history /active-time /observability), file sidebar. TRUE gaps: @-mention parsing/autocomplete (no @file/@repo/@artifact syntax), live graph lacks tool calls/queue state/costs/elapsed per node, no "Explain this run" timeline view, /export not implemented (no handler), no rerun/resume/rollback buttons. Master also researched a 5-mitigation matrix for delegation observability (staleness/heartbeat badges, heartbeat during blocking waits, narrate delegation, delegation progress pill, per-delegation cancel/nudge) — A/C/E cheap direct-build, B/D route planning. LESSON recorded: verify-before-building — 3 "missing" features already existed (webhook, observability dashboard, per-tool-call streaming); the true gap is master's ambient noise during waits, not absence of event plumbing.

