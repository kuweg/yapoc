1. I want to have support of all popular models providers, add placeholders in env.
2. Add centrilized agents config, where will be 
{
    "agent": name,
    "model": {
        "name": name,
        "key": key,
    },
    fallbacks: [
        "model": {
            "name": name,
            "key": key,
        },
        // based on DEFAULT_N_FALLBACKS_MODELS
    ]
} 

this config should dynamically fill keys, when system starts, and remove key values whyn system in existing and so on. Add .sh script that does this in any case - exit, user exit, crash and etc.
this file will be called `agent-settings-base.json` and stored in `app/agents/doctor`
copy of it will be `agent-settings.json` and stored and the `app/agents/doctor`

there should be script `heal` that recreates `agent-settings.json` from `agent-settings-base.json` in case of something is wrong. 
This script is accessible by doctor or separate sh script if doctor is not responding.

3. Add support for OpenAi, Google models, OpenRouters and local model via LLMStudio.
Write guide for LLMStudio for me

4. Perform Audit of agents and it's roles, provide a separate md file with report which consist of audit itself and improvements and optimization.
5. Fix token couting for agents - they should be able to count their own tokens as well as tool calls.